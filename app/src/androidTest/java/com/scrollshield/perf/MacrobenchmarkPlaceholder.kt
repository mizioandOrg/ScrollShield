package com.scrollshield.perf

import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.Test
import org.junit.runner.RunWith

/**
 * AndroidX Benchmark driver placeholder. The JVM-side CI gate lives in
 * PerfBenchmarkSuite under src/test/. This is the on-device entry point.
 */
@RunWith(AndroidJUnit4::class)
class MacrobenchmarkPlaceholder {
    @Test
    fun placeholder() { assert(true) }
}
