package com.scrollshield.integration

import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.Test
import org.junit.runner.RunWith

/**
 * Instrumented placeholder for SessionRecordingTest.
 *
 * Real behaviour is covered by Robolectric/JVM tests under src/test/.
 * Instrumented coverage requires a live emulator + accessibility service,
 * which are out of scope for CI gating.
 */
@RunWith(AndroidJUnit4::class)
class SessionRecordingTest {
    @Test
    fun harnessDiscoverable() { assert(true) }
}
